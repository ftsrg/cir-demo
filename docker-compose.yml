version: '3.8'
services:
  backend:
    image: ghcr.io/ftsrg/cir-demo-backend:latest
    environment:
      - BASIC_AUTH_FILE=/app/config/credentials.json

  frontend:
    image: ghcr.io/ftsrg/cir-demo-frontend:latest
    ports:
      - 3333:443
    depends_on:
      - backend
