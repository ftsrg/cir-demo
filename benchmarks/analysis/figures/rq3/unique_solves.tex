\begin{table}[t]
  \centering
  \caption{Unique solves over the tasks \texttt{c-nohavoc_std} shares with the C++ baseline (\texttt{cpp-baseline}). \emph{Solved} = a correct verdict from at least one verifier (BenchExec \texttt{correct}). \emph{c\_only} tasks are solved via the cir2c mapping but by no native-C++ verifier; \emph{cpp\_only} tasks are solved natively in C++ but by no verifier on the mapped-C input.}
  \label{tab:unique-solves}
\begin{tabular}{lrrrrrrr}
\toprule
 & shared & c\_solved & cpp\_solved & both & c\_only & cpp\_only & neither \\
suite &  &  &  &  &  &  &  \\
\midrule
c-nohavoc\_std & 980 & 623 & 916 & 583 & 40 & 333 & 24 \\
\bottomrule
\end{tabular}
\end{table}
